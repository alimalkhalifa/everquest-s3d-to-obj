{
  "name": "eq-s3d-to-obj",
  "version": "1.0.0",
  "main": "app/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.0.13",
    "@types/pako": "^1.0.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node app/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "nodemon -w src/ --ext ts,js,json --exec yarn -- dev",
    "postinstall": "yarn build"
  },
  "dependencies": {
    "pako": "^1.0.11"
  }
}
